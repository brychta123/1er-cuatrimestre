#include <stdio.h>
#include <stdlib.h>
#define <string.h>-a
#define T 2



int main()
{
    char legajo[T];
    char sueldo[T];
    char sexo[T];
    char nombres[T][20];
    int i;

    for(i=0;i<T; i++)
    {
        printf("ingrese legajo: ");
        scanf("%d", legajos[i]);
        printf("ingrese el sueldo: ");
        scanf("%f",sueldo);
        printf("ingrese sexo(m o f ):");
        scanf("%s", sexo);
        fflush(stdin);
        scanf("%c", &sexo[i]);
        pedirCadena("nombre: ", nombres[i], 20)

        sueldo[i]=sueldobruto
    }

    return 0;
}
